{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "npx --no-install serve -s dist -l $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
